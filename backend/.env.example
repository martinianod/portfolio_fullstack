# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5432/portfolio_crm
DATABASE_USER=portfolio
DATABASE_PASSWORD=portfolio_dev

# JWT Configuration (CHANGE IN PRODUCTION!)
JWT_SECRET=change-this-to-a-secure-random-string-at-least-256-bits-long
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# Admin User Configuration (CHANGE IN PRODUCTION!)
# Generate secure password: openssl rand -base64 32
ADMIN_EMAIL=admin@martiniano.dev
ADMIN_PASSWORD=admin123

# Email Configuration (Optional - uses mock if not configured)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS=true
CONTACT_RECIPIENT=admin@martiniano.dev

# CORS Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Application Configuration
SPRING_PROFILES_ACTIVE=dev
JPA_SHOW_SQL=false
